import styled from 'styled-components';

export const DivLevelMultiplayer = styled.div`
  
  h4 {
    color: #FFF;
  }
  .menu {
    display: flex;
    li {
      flex: 1;
    }
  }

  .create-host,
  .join-host {
    display: none;
  }

`;